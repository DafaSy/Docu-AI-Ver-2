import { AlertTriangle, Trash2, X } from 'lucide-react';
import type { DocumentRecord } from '../types';
export function DeleteDocumentModal({ document, loading, onClose, onConfirm }: { document?: DocumentRecord; loading?: boolean; onClose: () => void; onConfirm: () => void }) {
  if (!document) return null;
  return <div className="fixed inset-0 z-50 flex items-end bg-black/70 p-0 sm:items-center sm:justify-center sm:p-4"><div className="glass w-full rounded-t-2xl border border-danger-500/30 p-5 shadow-2xl sm:max-w-md sm:rounded-2xl"><div className="flex items-start justify-between"><div className="flex gap-3"><span className="rounded-xl bg-danger-500/15 p-2 text-danger-300"><AlertTriangle size={20} /></span><div><h3 className="font-semibold text-white">Delete document?</h3><p className="mt-1 text-xs text-ink-400">This removes the file and its library metadata.</p></div></div><button onClick={onClose} className="text-ink-400 hover:text-white"><X size={18} /></button></div><p className="mt-5 truncate rounded-lg border border-white/10 bg-black/20 px-3 py-2 text-sm text-ink-200">{document.file_name}</p><div className="mt-6 flex justify-end gap-2"><button onClick={onClose} disabled={loading} className="rounded-xl border border-white/10 px-4 py-2 text-sm text-ink-300">Cancel</button><button onClick={onConfirm} disabled={loading} className="inline-flex items-center gap-2 rounded-xl bg-danger-500 px-4 py-2 text-sm font-medium text-white disabled:opacity-50"><Trash2 size={15} />{loading ? 'Deleting…' : 'Delete'}</button></div></div></div>;
}
